{
    "version": 2,
    "builds": [
        {
            "src": "./index.ts",
            "use": "@vercel/node",
            "config": {
                "maxDuration": 60
            }
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}